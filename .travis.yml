language: python
python:
  - "2.7"
  - "3.3"
  - "3.4"
# command to install dependencies
before_install:
  - python --version
  - pip --version
# command to run tests
install:
  - pip install pytest --upgrade
  - pip install pytest-cov
script:
  - py.test --cov=myassert